import {
  StyledRoot,
  StyledContent,
  StyledClose,
  StyledOverlay,
  StyledTrigger,
} from './component';

export const Dialog = {
  Root: StyledRoot,
  Trigger: StyledTrigger,
  Content: StyledContent,
  Close: StyledClose,
  Overlay: StyledOverlay,
};
