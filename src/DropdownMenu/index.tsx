import {
  StyledContent,
  StyledArrow,
  StyledItem,
  StyledTrigger,
  StyledRoot,
} from './component';

export const DropdownMenu = {
  Root: StyledRoot,
  Arrow: StyledArrow,
  Content: StyledContent,
  Item: StyledItem,
  Trigger: StyledTrigger,
};
